
<%= form_for(@partnership) do |f| %>
  <% if @partnership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partnership.errors.count, "error") %> prohibited this partnership from being saved:</h2>

      <ul>
      <% @partnership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>



  <% end %>


 <div class="field">
<%= f.select :role, options_for_select(%w[Partner Observer Investor ]) %>
  </div>


 <div class="field">
    <%= f.text_field :user_id, :value => current_user.id, :type => "hidden" %>
  </div>


 <div class="field">
    <%= f.text_field :project_id, :value => @partnership.project_id, :type => "hidden" %>
  </div>


   <div class="actions">
    <%= f.submit name="Submit" %>
  </div>
<% end %>
