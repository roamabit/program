<%= form_for(@simvol) do |f| %>
  <% if @simvol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@simvol.errors.count, "error") %> prohibited this solution from being saved:</h2>

      <ul>
      <% @simvol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
